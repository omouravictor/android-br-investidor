<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@+id/nav_wallet">

    <fragment
        android:id="@+id/nav_wallet"
        android:name="com.omouravictor.invest_view.ui.wallet.WalletFragment"
        android:label="@string/menu_wallet"
        tools:layout="@layout/fragment_wallet">
        <action
            android:id="@+id/wallet_fragment_to_new_record_fragment"
            app:destination="@id/nav_new_record" />
    </fragment>

    <fragment
        android:id="@+id/nav_new_record"
        android:name="com.omouravictor.invest_view.ui.new_record.NewRecordFragment"
        android:label="@string/new_record_label"
        tools:layout="@layout/fragment_new_record" />

</navigation>